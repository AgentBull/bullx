[__meta__]
bcp47 = "en-US"

[app]
close = "Close"
actions = "Actions"

[app.connectivity]
client_lost_title = "We can't find the internet"
server_lost_title = "Something went wrong!"
reconnecting = "Attempting to reconnect"

[errors.validation]
required = "can't be blank"
cast = "is invalid"
format = "has invalid format"
inclusion = "is invalid"
exclusion = "is reserved"
subset = "has an invalid entry"
confirmation = "does not match confirmation"
acceptance = "must be accepted"

[errors.validation.length.string]
min = '''
.input {$count :integer}
.match $count
  1 {{should be at least 1 character}}
  * {{should be at least {$count} characters}}
'''
max = '''
.input {$count :integer}
.match $count
  1 {{should be at most 1 character}}
  * {{should be at most {$count} characters}}
'''
is = '''
.input {$count :integer}
.match $count
  1 {{should be 1 character}}
  * {{should be {$count} characters}}
'''

[errors.validation.length.binary]
min = '''
.input {$count :integer}
.match $count
  1 {{should be at least 1 byte}}
  * {{should be at least {$count} bytes}}
'''
max = '''
.input {$count :integer}
.match $count
  1 {{should be at most 1 byte}}
  * {{should be at most {$count} bytes}}
'''
is = '''
.input {$count :integer}
.match $count
  1 {{should be 1 byte}}
  * {{should be {$count} bytes}}
'''

[errors.validation.length.collection]
min = '''
.input {$count :integer}
.match $count
  1 {{should have at least 1 item}}
  * {{should have at least {$count} items}}
'''
max = '''
.input {$count :integer}
.match $count
  1 {{should have at most 1 item}}
  * {{should have at most {$count} items}}
'''
is = '''
.input {$count :integer}
.match $count
  1 {{should have 1 item}}
  * {{should have {$count} items}}
'''

[errors.validation.number]
less_than = "must be less than {$number}"
greater_than = "must be greater than {$number}"
less_than_or_equal_to = "must be less than or equal to {$number}"
greater_than_or_equal_to = "must be greater than or equal to {$number}"
equal_to = "must be equal to {$number}"
not_equal_to = "must be not equal to {$number}"

[users]
greeting = "Hello, {$name}!"

[gateway.feishu.auth]
activation_required = "Please activate this Feishu account by sending /preauth <code> in a private chat with the bot."
activation_success = "Activation complete. This Feishu account is now linked to BullX."
activation_code_invalid = "That activation code is invalid or expired."
activation_failed = "Activation failed. Please try again or ask an operator for a new code."
already_linked = "This Feishu account is already linked."
auto_match_available = "This Feishu account can be matched automatically. Please sign in again."
web_auth_created = "Use this login code: {$code}\nOpen {$login_url} to finish signing in."
web_auth_not_bound = "This Feishu account is not linked yet. Activate it with /preauth <code> first."
web_auth_failed = "Feishu login could not be started. Please try again later."
login_not_bound = "This Feishu account is not linked yet. Activate it from Feishu with /preauth <code> first."
denied = "This account is not allowed to use BullX."
direct_command_dm_only = "Please message the bot privately to link or sign in."

[gateway.feishu.ping]
pong = "PONG!"

[gateway.feishu.delivery]
fallback_text = "[message]"
stream_generating = "Generating..."
stream_failed = "The streamed response failed."
stream_cancelled = "The streamed response was cancelled."
reply_target_missing_sent_to_scope = "Reply target was missing; sent to the chat instead."

[gateway.feishu.errors]
unsupported_message = "[unsupported Feishu message]"
profile_unavailable = "Feishu user profile is unavailable."
