# Copy this file to .env.local (gitignored) for personal overrides that take
# precedence over .env.dev in dev mode.
# Use .env.dev / .env.test (both committed) for shared team defaults.

# Example contents:
# FEISHU_APP_ID=foo
# FEISHU_APP_SECRET=bar
